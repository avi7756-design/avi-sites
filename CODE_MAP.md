# CODE_MAP.md — מיפוי ארכיטקטורת מרכז השיווק

> נוצר: 09/06/2026 · גרסה: v3.2

---

## 1. קבצים ותפקידים

| קובץ | תפקיד | שורות |
|-------|--------|-------|
| `manage.html` | **מרכז הניהול הראשי** — 8 לשוניות: היום, הודעות, שו"ת, מדיה, לידים, התנגדויות, ערוצים, קישורים | ~1,032 |
| `dashboard.html` | דשבורד לידים עצמאי — KPI, גרף מקורות, עריכה/מחיקה | ~189 |
| `lead-entry.html` | טופס רישום ליד מהיר (מובייל) — לחיצה ולא הקלדה | ~126 |
| `dira-gallery/index.html` | דף נחיתה לדירה — גלריה, שו"ת, טופס ליד, WhatsApp | ~228 |
| `dira-gallery/media.json` | קטלוג מדיה — סרטונים (3), קבצים (2), טקסט WhatsApp | 15 |
| `version.js` | מספר גרסה גלובלי (v3.2) | 1 |
| `manifest.json` | PWA manifest — מאפשר התקנה כאפליקציה | 14 |
| `facebook-posts/*.txt` | 9 גרסאות פוסטים לפייסבוק (קבצים נפרדים) | ~9 |

---

## 2. ארכיטקטורה — איך הכל מתחבר

```
┌─────────────────────────────────────────────────┐
│              manage.html (מרכז ראשי)            │
│  ┌─────┬─────┬─────┬─────┬─────┬─────┬─────┐   │
│  │היום │הודע.│שו"ת│מדיה │לידים│התנגד│ערוצי│   │
│  │     │     │    │     │     │     │פרסום│   │
│  └──┬──┴─────┴────┴──┬──┴──┬──┴─────┴──┬──┘   │
│     │                │     │            │       │
│     ▼                ▼     ▼            ▼       │
│ localStorage     media.json  n8n API   localStorage │
│ (משימות יום,     (סרטונים,  (לידים)   (ערוצים,     │
│  מעקב לידים,      עלונים)             קישורים)     │
│  התנגדויות)                                         │
└─────────────────────────┬───────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
  dashboard.html    lead-entry.html   dira-gallery/
  (דשבורד לידים)    (רישום מהיר)      (דף נחיתה)
        │                 │                 │
        └────────┬────────┘                 │
                 ▼                          ▼
         n8n webhooks ◄─────────────────────┘
         (6 endpoints)
```

---

## 3. n8n Endpoints (6)

| Endpoint | שיטה | תפקיד | קורא מ... |
|----------|-------|--------|-----------|
| `leads-data-eilat5` | GET | שליפת כל הלידים | manage, dashboard |
| `lead-update-eilat5` | POST | עדכון ליד קיים | manage, dashboard |
| `lead-delete-eilat5` | POST | מחיקת ליד | manage, dashboard |
| `new-lead-eilat5` | POST | יצירת ליד חדש | lead-entry, dira-gallery, manage (ייבוא) |
| `state-get-eilat5` | GET | שליפת מצב (יומי/ערוצים/קישורים) | manage |
| `state-set-eilat5` | POST | שמירת מצב לשרת | manage |

**כל קריאה = הרצה ב-n8n.** במכסה חינמית (~300/חודש) זה מגביל.

---

## 4. מבני נתונים מרכזיים

### CHANNELS (30 ערוצי פרסום)
```js
{n:"שם הערוץ", t:"site|paid|fb|fbpage|forum|wa", p:1-3, l:"URL"}
```
סוגים: site (לוח), paid (בתשלום), fb (קבוצת פייסבוק), fbpage (דף עסקי), forum, wa (וואטסאפ)

### GROUPS (14 קבוצות פייסבוק לרוטציה יומית)
```js
{n:"שם הקבוצה", l:"URL"}
```
**רוטציה:** `day*2 % 14` ו-`day*2+1 % 14` — כל יום 2 קבוצות שונות, מתחלפות.

### GCHIDX (מיפוי קבוצה→ערוץ)
מערך של 14 אינדקסים — כאשר משימת "בוצע" מסמנת גם את הערוץ המתאים בלשונית ערוצים.

### POSTS (5 גרסאות פוסטים)
```js
{v:"A-E", name:"תיאור", body:"תוכן מלא"}
```
**רוטציה:** `day % 4` — מתחלף אוטומטית (A/B/C/D, E קצרה לוואטסאפ).

### QA (14 שאלות ותשובות)
מערך זוגות `[שאלה, תשובה]` — מוצג בלשונית שו"ת + כפתור העתקה.

### מצב היום (localStorage + n8n sync)
```js
{
  date: "2026-06-09",
  tasksDone: [1,2,3],   // מספרי משימות שבוצעו
  skip: [4],             // משימות שדולגו
  log: [{ts, num, action, by}],  // יומן פעולות
  total: 5,
  updatedBy: "phone"|"desktop"
}
```

### מעקב לידים (localStorage בלבד — eilat_leadlog)
```js
{
  "phonenumber": {
    temp: "hot"|"warm"|"cold"|null,
    obj: ["מחיר","גודל"],     // סוגי התנגדות
    todos: [{action, due, done}],
    log: [{d:"תאריך", t:"טקסט"}]
  }
}
```

---

## 5. זרימות עיקריות

### ליד נכנס
1. **דף נחיתה** → משתמש משאיר טלפון → fetch ל-`new-lead-eilat5` → n8n Data Table
2. **רישום ידני** → lead-entry.html → אותו webhook
3. **ייבוא רשימה** → manage.html → parsing + בדיקת כפילויות → webhook לכל ליד

### ליד מתעדכן
1. לחיצה על ליד → מודאל → עריכת שדות → `lead-update-eilat5` → n8n מעדכן
2. **במקביל:** temperature/notes/todos/objections → localStorage בלבד (לא מסונכרן!)

### סנכרון בין מכשירים
- **מצב יומי:** `state-set-eilat5` שומר → `state-get-eilat5` טוען בכניסה
- **ערוצים + קישורים:** אותו מנגנון
- **מעקב לידים (temperature, notes):** **לא מסונכרן** — נשמר רק ב-localStorage

### משימות יום
1. בוקר: manage.html → `pullToday()` → טוען מ-n8n → מציג משימה ראשונה
2. משתמש: מבצע → "בוצע" → שומר ב-localStorage + n8n → מעביר לבאה
3. משימה מסוג "pub": גם מסמנת את הערוץ המתאים ב-CHANNELS

---

## 6. נקודות תורפה וסיכונים

| # | נקודה | חומרה | הסבר |
|---|--------|--------|------|
| 1 | **מכסת n8n** | 🔴 גבוהה | כל פעולה = הרצה. כניסה ל-manage.html = ~4 קריאות (leads + 3x state-get). מכסה חינמית ~300/חודש |
| 2 | **localStorage = המוח** | 🔴 גבוהה | temperature, notes, todos, objection tagging — כולם רק בדפדפן. ניקוי cache = איבוד מלא |
| 3 | **אין אימות** | 🟡 בינונית | כל webhook פתוח לכל. מי שיש לו URL יכול לקרוא/למחוק לידים |
| 4 | **קוד כפול** | 🟡 בינונית | manage + dashboard — שניהם מממשים CRUD לידים באופן נפרד. שינוי בהגיון דורש עדכון בשניים |
| 5 | **אין אנליטיקס** | 🟡 בינונית | אי אפשר לדעת: איזה ערוץ מייצר הכי הרבה לידים, מה שיעור ההמרה, ROI |
| 6 | **sync race** | 🟡 בינונית | שני מכשירים בו-זמנית = last-write-wins. אין merge |
| 7 | **אין offline** | 🟢 נמוכה | n8n למטה = מסך שגיאה בלידים. אין cache fallback |
| 8 | **קובץ אחד ענק** | 🟢 נמוכה | manage.html = 1,032 שורות inline. קשה לתחזק, diffs גדולים |
| 9 | **אין גיבוי** | 🟡 בינונית | אין כפתור "ייצוא" למעקב לידים. אין snapshot |
| 10 | **איפוס ידני** | 🟢 נמוכה | ערוצים "מתאפסים כל בוקר" — למעשה רק אם השרת מסנכרן. אין cron אמיתי |

---

## 7. רשימת שיפורים ממוינת לפי השפעה

### שיפורי המרה (יותר לידים/ביקורים)

| # | שיפור | השפעה עסקית | מאמץ | סיכון |
|---|--------|-------------|------|-------|
| H1 | **שליחה ישירה לוואטסאפ/פייסבוק מהמערכת** — כפתור "שלח" שפותח WhatsApp Web/FB עם הטקסט מוכן | ⭐⭐⭐⭐⭐ | בינוני | נמוך |
| H2 | **דשבורד ROI לפי ערוץ** — כמה לידים מכל מקור, שיעור המרה, עלות-לליד | ⭐⭐⭐⭐ | בינוני | נמוך |
| H3 | **A/B testing אוטומטי** — עוקב איזו גרסת פוסט (A/B/C/D) מייצרת יותר לידים | ⭐⭐⭐ | גבוה | נמוך |

### שיפורי שימושיות (קל יותר לאבי בשטח)

| # | שיפור | השפעה עסקית | מאמץ | סיכון |
|---|--------|-------------|------|-------|
| U1 | **כפתור "שלח עכשיו" בקבוצות** — במקום העתק→מעבר לפייסבוק→הדבק, כפתור אחד שפותח share dialog | ⭐⭐⭐⭐⭐ | נמוך | נמוך |
| U2 | **סנכרון מלא של מעקב לידים** — temperature, notes, todos → n8n (לא רק localStorage) | ⭐⭐⭐⭐ | בינוני | בינוני |
| U3 | **התראות push** — "ליד חם לא טופל 2 שעות" / "הגיע ליד חדש" דרך n8n→Telegram | ⭐⭐⭐⭐ | בינוני | נמוך |
| U4 | **ייצוא/גיבוי אוטומטי** — כפתור "הורד Excel" + גיבוי יומי ל-Google Sheets | ⭐⭐⭐ | בינוני | נמוך |
| U5 | **עיצוב כהה (dark mode)** — כמו דף הנחיתה החדש, לחוויה אחידה | ⭐⭐ | גבוה | נמוך |

### שיפורים טכניים (יציבות, מכסה, ביצועים)

| # | שיפור | השפעה עסקית | מאמץ | סיכון |
|---|--------|-------------|------|-------|
| T1 | **צמצום קריאות n8n** — cache לידים ב-localStorage עם TTL, batch state calls | ⭐⭐⭐⭐ | בינוני | נמוך |
| T2 | **אימות בסיסי** — API key פשוט ב-header לכל webhook | ⭐⭐⭐ | נמוך | נמוך |
| T3 | **פירוק manage.html למודולים** — JS חיצוני, CSS חיצוני, קל יותר לתחזק | ⭐⭐ | גבוה | בינוני |
| T4 | **Service Worker + offline cache** — עובד גם בלי אינטרנט | ⭐⭐ | גבוה | בינוני |

---

## 8. המלצה: 3 שיפורים ראשונים

### 1. 🥇 כפתור "שלח עכשיו" — שליחה ישירה מהמערכת (U1 + H1)
**למה:** זה ה-friction הגדול ביותר. היום: העתק → עבור לפייסבוק → הדבק → חזור. עם כפתור "שלח": לחיצה אחת → WhatsApp/Facebook נפתחים עם הטקסט מוכן + תמונות מצורפות.
**מה:** כפתור `wa.me` share (כבר קיים חלקית), + Web Share API עבור פייסבוק groups, + כפתור `mailto:` עבור מייל.
**מאמץ:** נמוך-בינוני. **סיכון:** נמוך.

### 2. 🥈 דשבורד ROI/סטטיסטיקה (H2)
**למה:** אבי לא יודע מה עובד ומה לא. "פרסמתי ב-14 קבוצות — מה הביא לידים?" בלי מדידה, אי אפשר לייעל.
**מה:** עמודה חדשה בלידים: "מאיזה ערוץ ספציפי הגיע" + דשבורד גרפי (לידים לפי ערוץ, אחוז המרה לפי מקור, cost per lead).
**מאמץ:** בינוני. **סיכון:** נמוך.

### 3. 🥉 צמצום קריאות n8n + cache (T1)
**למה:** כל כניסה ל-manage.html שולחת ~4 קריאות. פתיחה 10 פעמים ביום = 40 הרצות. בחודש = ~1,200 רק מכניסות, בלי עדכוני לידים. מכסה חינמית תיגמר.
**מה:** שמירת לידים ב-localStorage עם TTL של 5 דקות, batch בקשות state, כפתור "רענן" ידני במקום אוטומטי.
**מאמץ:** בינוני. **סיכון:** נמוך.

---

> **⏸️ עצירה — נא לבחור מה לבצע.**
